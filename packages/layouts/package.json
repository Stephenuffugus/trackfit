{
  "name": "@trackfit/layouts",
  "version": "0.3.0-dev",
  "private": true,
  "description": "Trackfit rule-based layout suggester: matches a hobbyist's track inventory against a hand-tuned catalog of named layout templates and reports buildability + shopping-list shortfalls. Pure TypeScript, no LLM, no web access.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trackfit/library": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
