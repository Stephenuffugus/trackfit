{
  "name": "@trackfit/measure",
  "version": "0.3.0-dev",
  "private": true,
  "description": "Trackfit reference-object gap measurement: pixel-distance ratios from a known object in frame to derive real-world gap length. Pure TS math, no DOM/canvas/CV deps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
