{
  "name": "@trackfit/solver",
  "version": "0.3.0-dev",
  "private": true,
  "description": "Trackfit solver: subset-sum and (forthcoming) curve-aware fitting for sectional track gaps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
