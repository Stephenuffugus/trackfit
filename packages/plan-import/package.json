{
  "name": "@trackfit/plan-import",
  "version": "0.3.0-dev",
  "private": true,
  "description": "Trackfit plan import — parses AnyRail XML plans and produces a buy list against the user's inventory.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trackfit/library": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
